spring:
  application:
    name: sellersprite-service
  batch:
    job:
      enabled: false
    jdbc:
      initialize-schema: always
  ai:
    tools:
      throw-exception-on-error: true
    chat:
      memory:
        repository:
          jdbc:
            initialize-schema: always
    model:
      chat: none
      embedding: none
      image: none
      audio:
        speech: none
        transcription: none
      moderation: none
    openai:
      api-key:
      base-url: https://yuanbaomao.cyou/v1
      chat:
        model: gpt-5.5
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:sellersprite_service_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  data:
    redis:
      repositories:
        enabled: false

mybatis-plus:
  global-config:
    banner: false

ybm:
  mybatis:
    mapper:
      base-packages:
        - com.yuanbaomao.sellersprite.db.mapper

sellersprite:
  research:
    recovery-enabled: false
